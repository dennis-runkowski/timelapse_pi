#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='timelapse',
    version='0.1',
    description='Create a timelapse video from your raspberry pi!',
    packages=find_packages()
)